import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#f25822",
      contrastText: "#ffff",
    },
    secondary: {
      main: "#242B33",
      contrastText: "#ffff",
    },
  },
});

export default theme;
